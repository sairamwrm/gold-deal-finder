name: Heartbeat

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: |
          date -u
          echo "Heartbeat schedule OK"
